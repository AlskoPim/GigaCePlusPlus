#include <iostream>
#include <string>

using namespace std;

int main(){

    string liczbaS = "";
    int liczba = 0;

    cout << "Podaj liczbe : ";
    getline(cin, liczbaS);
    liczba = stoi(liczbaS);
    cout << liczba << endl;

    return 0;
}
